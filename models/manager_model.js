const dbe = require(__dirname + "../../modules/mysql-excute");
const {body, header, checkSchema}  = require("express-validator");


const managerValidator = (loginData) => {
    try {

    } catch (error) {

    }
}